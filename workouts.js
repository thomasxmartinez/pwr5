'use strict';
function calcLifts(element) {
  document.getElementById('total');
  document.getElementById('submission');
}

console.log(calcLifts);

function createArray(element) {
  var newArray = [];
  newArray.push(document.getElementsById('total'));
  newArray.push(document.getElementsById('submission'));
}

console.log(createArray);
